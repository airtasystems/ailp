/**
 * Default origin for the AIRTA-hosted AILP API (no trailing slash).
 * Override with `NEXT_PUBLIC_AILP_BASE_URL` / `VITE_AILP_BASE_URL`, or pass
 * `baseUrl` to `createAilp()` / `useAilp()`, when using a self-hosted instance.
 */
export declare const AILP_DEFAULT_BASE_URL = "https://airtasystems.com/ailp";
//# sourceMappingURL=constants.d.ts.map